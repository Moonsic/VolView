// import { get, post, baseUrl } from '@/api/axios/request'

// 语言管理相关接口（1条） - language

// 1. 缓存语言(没有用到了, 因为直接在连接websocket时把语言也发给后台)
// export const cacheLanguageApi = (
//   params: {
//     lang: string // zh-hans | en
//   }
// ) => post(`${baseUrl}/language/cacheLanguage`, params)
